#!/bin/bash

# Exit immediately if a command exits with a non-zero status
set -e

# TODO: Configure and build SDL as a static library
# TODO: Configure and build SDL_shadercross as a static, vendored, CLI executable
